public class ActionAdd extends Action implements IAction {

    private static final String ACTION_NAME = "Add a book.";
    protected ActionAdd(Collection mCollection) {
        super(mCollection, ACTION_NAME);
    }

    @Override
    public void doAction() {

    }
}
